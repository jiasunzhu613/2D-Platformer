using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Player
{
    public enum PlayerState{
        STATE_STANDING = 0,
        STATE_WALLCLIMBING = 1,
        STATE_JUMPING = 2,
        STATE_DASHING = 3
    }
}
